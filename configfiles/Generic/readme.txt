Flashing ROOter
-----------------------

When flashing from the factory firmware use the image with "factory"
in its name.

When upgrading ROOter to a new version use the image with "update"
in its name only.